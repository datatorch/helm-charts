# apiVersion: v1
# kind: PersistentVolumeClaim
# metadata:
#   name: {{ include "datatorch.fullname" . }}-data
# spec:
#   accessModes:
#     - ReadWriteMany
#   storageClassName: {{ .Values.backend.pvc.storageClassName }}
#   resources:
#     requests:
#       storage: {{ .Values.backend.pvc.storage }}
